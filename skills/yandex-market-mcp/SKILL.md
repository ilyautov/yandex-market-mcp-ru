---
name: yandex-market-mcp
description: "Подключить Яндекс Маркет к ИИ-ассистенту и работать с кабинетом партнёра прямо из чата. Ставит MCP-сервер `yandex-market-mcp-ru`: 165 методов официального API, каталог ищется обычными словами, у каждого метода класс доступа, запись требует подтверждения. Используй, когда человек хочет автоматизировать работу с кабинетом, собрать аналитику по продажам или разобраться с API. Триггеры: «подключи яндекс маркет», «api яндекс маркета», «partner api маркета», «заказы с маркета», «цены на яндекс маркете», «yandex market mcp»."
---

# Яндекс Маркет через MCP

Ставит сервер `yandex-market-mcp-ru`: 165 методов официального API
(чтение 109, запись 49, необратимое 7). Каталог методов лежит в YAML и
исполняется сервером, поэтому агент получает несколько универсальных
инструментов, а не по отдельной функции на каждый метод.

## Установка

Первый релиз на PyPI выпускается тегом `v0.5.3`, до этого пакет ставится прямо из репозитория:

```bash
uvx --from git+https://github.com/ilyautov/yandex-market-mcp-ru yandex-market-mcp-ru
```

После релиза строка короче:

```bash
uvx yandex-market-mcp-ru
```

Конфигурация клиента (Claude Desktop, Cursor, Claude Code):

```json
{
  "mcpServers": {
    "ym": {
      "command": "uvx",
      "args": ["--from", "git+https://github.com/ilyautov/yandex-market-mcp-ru", "yandex-market-mcp-ru"],
      "env": {
        "YANDEX_MARKET_API_KEY": "<значение>"
      }
    }
  }
}
```

## Где взять ключи

- `YANDEX_MARKET_API_KEY`: API-ключ из кабинета партнёра, раздел Настройки → API.

Второй путь, без переменных окружения: поднять сервер и вызвать
`ym_add_cabinet`. Ключи лягут в `~/.marketplace-mcp/cabinets.json`
с правами 600 и с машины не уходят. Так же подключается второй магазин.

## Что спрашивать у сервера

- `ym_search_methods("остатки по складам")`: поиск метода словами.
- `ym_describe_method(...)`: параметры, пагинация, класс доступа.
- `ym_call_method(...)`: вызов. Чтение идёт сразу, запись просит
  подтверждения, необратимое просит дважды.
- `ym_check_auth`: проверить, что ключи дошли.

## Как себя вести

Сначала ищи метод каталогом, не угадывай имя. Перед записью покажи человеку,
что именно уйдёт в API, и дождись согласия: гейт сработает всё равно, но
человек должен увидеть тело запроса до, а не после.

Числа из ответа не округляй и не пересчитывай в уме. Если метод вернул
пустой список, так и скажи, вместо того чтобы объяснять это за продавца.

## Все четыре маркетплейса разом

Если нужны не только Яндекс Маркет, ставится комбайн `marketplaces-mcp-ru`:
Wildberries, Ozon, Яндекс Маркет и Авито в одном сервере.
